@model RoleViewModel
@{
	ViewData["Title"] = "Edit";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit Role  : @Model.Name</h1>
@* @if (Model.Imagename is not null)
{
	//<div class="form-control">
	<img class="mt-3" src="~/Files/Images/@Model.Imagename" width="200" height="200" alt="Employee's Image'" class="rounded" />
	//</div>
} *@
<div asp-validation-summary="All"></div>
<form Method="Post" asp-action="Edit" enctype="multipart/form-data">

	<partial name="PartialView/Create_EtidPartialView"></partial>

	<div class="mt-3">
		<input type="submit" asp-route-id="@Model.Id.ToString()" value="Edit" class="btn btn-info" />
		<a asp-action="AddOrRemoveUser" asp-route-roleId="@Model.Id.ToString()" class="btn btn-info">Add or Remove User</a>
		<a asp-action="Index" class="btn btn-info">Cancel</a>
	</div>

</form>
